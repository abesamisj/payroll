@model IEnumerable<Payroll.Models.IncomeViewModels>
@using GridMvc.Html
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Incomes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IncomeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncomeValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncomeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncomeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        @*<th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr onclick="location.href = '@(Url.Action("Edit", "Income", new { id = item.IncomeId }, null))'">
            <td>
                @Html.DisplayFor(modelItem => item.IncomeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncomeValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncomeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncomeDescription)
            </td>
            <td>
                @if (item.Active)
                {
                    <p>Active</p>
                }
                else
                {
                    <p>In Active</p>
                }
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IncomeId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IncomeId })
                </td>
        </tr>
    }

</table>*@
<div class="code-cut">
    @Html.Grid(Model).Columns(columns =>
                    {
                        columns.Add(c => c.IncomeId).Titled("Income Id").Filterable(true);
                        columns.Add(c => c.IncomeName).Titled("Income Name").Filterable(true);
                        columns.Add(c => c.IncomeValue).Titled("Income Value").Filterable(true);
                        columns.Add(c => c.IncomeDescription).Titled("Income Description");
                        columns.Add(c => c.Active).Titled("Status");
                        columns.Add()
                        .Encoded(false)
                        .Sanitized(false)
                        .SetWidth(30)
                        .RenderValueAs(o => Html.ActionLink("Edit", "Edit", new { id = o.IncomeId }));

                    }).WithPaging(20).Sortable(true)
</div> 